alembic revision --autogenerate -m "create users table"  # create migration
alembic upgrade head # upgrade database
alembic current        # current version db
alembic history        # migration history
alembic downgrade -1   # back to before level
alembic downgrade base # back to first
